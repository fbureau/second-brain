# 04-meetings/

Ingested, structured meetings. Decisions, action items, dynamics. Re-referenced over time.

## Naming convention

`YYYY-MM-DD-slug.md` (the slug reflects the topic, not the format).

## Schema

See `_CLAUDE.md` at the vault root, type schema `meeting`.

## Associated skill

`meeting-ingest`.
