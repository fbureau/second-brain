# AGENTS.md — entry point for agents that auto-load this file

This folder is a **second-brain vault**. Before doing anything here:

1. Read `_CLAUDE.md` (vault structure, AI-first rules, note schemas, what you never do).
2. Read `00-inbox/MY-PROFILE.md` (who the user is, working language, sensitive topics).

If the `second_brain` tools are available (Hermes plugin), call `sb_brief` instead of reading
both files in full, and write notes only through those tools. Never delete a note — move it to
`07-archive/`. `02-people/` and `05-decisions/` are append-only.
